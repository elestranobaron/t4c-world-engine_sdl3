# --- Build CMake / SDL3 ---
/build/
/_deps/
/out/
cmake-build-*/
# --- Objets / binaires ---
*.o
*.a
*.so
*.exe
mapi_sdl3
# --- Assets T4C (NE PAS publier — contenu propriétaire) ---
/data/
/data/**
*.dec
*.vsf
*.rmap
*.vsb
# --- Symlinks locaux (chemins absolus vers path_to_follow) ---
# Copier le vrai code dans TnC_dev/ avant de committer, puis retirer ces lignes.
# TnC_dev/FontManager
# TnC_dev/VSFInterface
# TnC_dev/NPCManager
# TnC_dev/TextManager
# TnC_dev/fonts
# --- Fichiers temporaires ---
*.swp
*~
.DS_Store
Thumbs.db
*.log
